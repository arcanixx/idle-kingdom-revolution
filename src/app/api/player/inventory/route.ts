"import { NextRequest } from \"next/server\"; import { createApiClient, jsonResponse, errorResponse } from \"@/lib/supabase/api-helper\"; export async function GET(request: NextRequest) { const supabase = await createApiClient(); const { data: { user }, error: authError } = await supabase.auth.getUser(); if (authError || !user) return errorResponse(\"Unauthorized\", 401); return jsonResponse({ inventory: [] }); }" 
